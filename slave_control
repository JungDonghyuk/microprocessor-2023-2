#include <SPI.h>

volatile uint8_t RECEICVED;// 최적화 막기 위해 uint대신 volatile사용

void setup() {
  SPI.begin();
  SPI.attachinterrupt();
  DDRB |= 0xFF;
}

ISR (SPI_STC_vect) {
  DDRB |= 0xFF;
  RECEICVED = SPDR; 
  PORTB |= RECEICVED; 
  Serial.println(RECEIVED);
}
